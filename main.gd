extends Node

# scope

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	
	# var godot dan coolness cmn ada di func _ready
	# tipe variabel bisa tanpa di declare
	var godot = true
	var coolness = 9001
	
